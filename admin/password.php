<?php
require_once __DIR__.'/../includes/layout.php'; require_once __DIR__.'/../includes/auth.php';$admin=require_admin();if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();if(strlen($_POST['password']??'')<10){flash('danger','Use at least 10 characters.');}else{db()->prepare('UPDATE admins SET password_hash=? WHERE id=?')->execute([password_hash($_POST['password'],PASSWORD_DEFAULT),$admin['id']]);audit($admin['id'],'change_password');flash('success','Password changed.');}redirect(base_url('admin/password.php'));}page_header('Change Password',true);
?><div class="narrow" style="margin:auto"><div class="card"><h1>Change Password</h1><form method="post"><?=csrf_field()?><label>New Password<input name="password" type="password" minlength="10" required></label><button class="btn btn-primary">Update Password</button></form></div></div><?php page_footer(); ?>
